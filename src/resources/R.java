package resources;

public class R {
    
    public enum strings {
    
        welcome_message,
        app_name,
        new_key,
        wow_key,
        the_end,
        alpha
    }

    public enum colors {
    
        primary_color,
        secondary_color
    }
}
