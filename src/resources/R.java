package resources;

public class R {
    
    public enum strings {
    
        welcome_message,
        app_name,
    }

    public enum colors {
    
        primary_color,
        secondary_color
    }
}
